import { theme as base } from "@chakra-ui/react"

// Adobe typekit loaded by gatsby-plugin-web-font-loader
const fonts = {
  heading: `dunbar-tall, ${base.fonts.heading}`,
  body: `dunbar-tall, ${base.fonts.body}`,
}

export default fonts
